<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a4ic1.projektkoncowyciborowski">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CameraActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".AlbumActivity" />
        <activity android:name=".KolazActivity" />
        <activity android:name=".SiecActivity" />
        <activity android:name=".PicturesActivity" />
        <activity android:name=".SinglePictureActivity" />
        <activity android:name=".ChooseCollageActivity" />
        <activity android:name=".CollageActivity" />
        <activity android:name=".CollageCameraActivity" />
        <activity android:name=".LettersActivity"></activity>
    </application>

</manifest>